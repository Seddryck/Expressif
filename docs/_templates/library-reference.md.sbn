---
layout: docs
title: "{{ model.name }}"
parent: "{{ model.parent_title }}"
grand_parent: "{{ model.grand_parent_title }}"
nav_order: {{ model.nav_order }}
has_toc: false
permalink: /{{ model.kind_plural }}/{{ model.scope_slug }}/{{ model.name }}/
tags:
  - {{ model.kind_plural }}
  - {{ model.scope_slug }}
generated: true
---

```
{{ model.signature }}
```

{{ model.summary }}

{{ if model.deprecated }}
> **Deprecated:** Use `{{ model.replacement }}` instead. This function is planned for removal in Expressif {{ model.sunset }}.
{{ end }}

## Parameters

{{ if model.parameters.size == 0 }}

This {{ model.kind }} has no parameters.

{{ else if model.has_parameter_types }}

| Name | Type | Required | Description |
|:-----|:-----|:---------|:------------|
{{ model.parameter_rows }}

{{ else }}

| Name | Required | Description |
|:-----|:---------|:------------|
{{ model.parameter_rows }}
{{ end }}

{{ if model.has_traversal || model.has_evaluation -}}

## Argument evaluation

{{ if model.has_traversal -}}
Traverses `{{ model.traversal_source }}` → `{{ model.traversal_selection }}`.

{{ end -}}
{{ for parameter in model.parameters -}}
{{ if parameter.evaluation_frequency == "once" -}}
- **`{{ parameter.name }}`:** Evaluated once against `{{ parameter.evaluation_source }}` → `{{ parameter.evaluation_selection }}`.
{{ else if parameter.evaluation_frequency == "per-element" -}}
- **`{{ parameter.name }}`:** Evaluated once against each selected element of the traversal.
{{ end -}}
{{ end -}}

{{ end -}}

{{~ if model.has_behavior }}

## Behavior

{{ model.behavior }}
{{ end ~}}

{{ if model.examples.size > 0 }}

## Examples

{% raw %}
```expressif
{{ model.examples_text }}
```
{% endraw %}
{{ end }}

{{ model.reference_text }}
{: .member-reference }
