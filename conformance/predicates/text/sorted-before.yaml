suite: text
kind: predicate
operator: is-sorted-before
tests:
  - id: sorted-before.valid
    cases:
      - id: sorted-before.valid.text.same
        value: A
        expected: false
        parameters:
          - A
      - id: sorted-before.valid.text.before
        value: A
        expected: true
        parameters:
          - B
      - id: sorted-before.valid.text.after
        value: B
        expected: false
        parameters:
          - A
      - id: sorted-before.valid.empty-string.special.empty
        value: ""
        expected: false
        parameters:
          - (empty)
      - id: sorted-before.valid.string.special.empty
        value: A
        expected: false
        parameters:
          - (empty)
      - id: sorted-before.valid.null.special.null
        value: (null)
        expected: false
        parameters:
          - (null)
