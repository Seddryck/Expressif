suite: text
kind: predicate
operator: is-equivalent-to
tests:
  - id: is-equivalent-to.valid
    cases:
      - id: is-equivalent-to.valid.text.same
        value: A
        expected: true
        parameters:
          - A
      - id: is-equivalent-to.valid.text.before
        value: A
        expected: false
        parameters:
          - B
      - id: is-equivalent-to.valid.text.after
        value: B
        expected: false
        parameters:
          - A
      - id: is-equivalent-to.valid.empty-string.special.empty
        value: ""
        expected: true
        parameters:
          - (empty)
      - id: is-equivalent-to.valid.string.special.empty
        value: A
        expected: false
        parameters:
          - (empty)
      - id: is-equivalent-to.valid.null.special.null
        value: (null)
        expected: true
        parameters:
          - (null)
