suite: array
kind: predicate
operator: some
tests:
  - id: some.valid.predicate
    cases:
      - id: some.valid.predicate.array.one-match
        value: "{1,2,3}"
        expected: true
        parameters:
          - equal-to(2)
      - id: some.valid.predicate.array.several-matches
        value: "{1,2,3,4}"
        expected: true
        parameters:
          - even
      - id: some.valid.predicate.array.no-match
        value: "{1,2,3}"
        expected: false
        parameters:
          - greater-than(5)
      - id: some.valid.predicate.array.empty
        value: "{}"
        expected: false
        parameters:
          - even
      - id: some.valid.predicate.special.null
        value: (null)
        expected: false
        parameters:
          - even
      - id: some.valid.predicate.special.empty
        value: (empty)
        expected: false
        parameters:
          - even
