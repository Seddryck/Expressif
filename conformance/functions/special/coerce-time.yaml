suite: special
kind: function
operator: coerce-time
tests:
  - id: coerce-time.valid
    cases:
      - id: coerce-time.valid.special.null
        value: (null)
        expected:
      - id: coerce-time.valid.special.empty
        value: (empty)
        expected:
      - id: coerce-time.valid.text.canonical
        value: "14:30:00"
        expected: "14:30:00"
      - id: coerce-time.valid.text.midnight
        value: "00:00:00"
        expected: "00:00:00"
      - id: coerce-time.valid.text.invalid
        value: invalid
        expected:
  - id: coerce-time.valid.date-only
    cases:
      - id: coerce-time.valid.date-only.midnight
        value: "2026-09-01"
        expected: "00:00:00"
