interface:
  display_name: "Install local CLI"
  short_description: "Extract a PR or release CLI locally"
  default_prompt: "Use $install-local to install the portable .NET 10 Windows x64 Expressif CLI from a pull request or release into a local directory I specify."
